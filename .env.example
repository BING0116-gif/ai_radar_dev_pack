# AI Radar 环境变量示例
# 使用方法：复制本文件为 .env 并填写实际值。
# 安全要求：禁止将真实密钥提交到 Git；.env 已被 .gitignore 排除。

# 运行环境：development / production
APP_ENV=development

# 后端服务
APP_HOST=127.0.0.1
APP_PORT=8000

# 后续 Card 会逐步补充 LLM API Key 等配置项（均留空占位）。
# LLM_API_KEY=